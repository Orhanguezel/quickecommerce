import PaymentSettings from "@/components/screen/admin-section/payment-settings";

const PaymentGetWaysRoot = () => {
  return (
    <PaymentSettings />
  );
};

export default PaymentGetWaysRoot;
