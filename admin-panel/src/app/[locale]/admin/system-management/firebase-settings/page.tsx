import FirebaseSettings from "@/components/screen/admin-section/firebase-settings";

const FirebaseSettingsRoot = () => {
  return (
    <FirebaseSettings />
  );
};

export default FirebaseSettingsRoot;