import PaymentSettings from "@/components/screen/admin-section/payment-settings";

const PaymentSettingsRoot = () => {
  return (
    <PaymentSettings />
  );
};

export default PaymentSettingsRoot;
