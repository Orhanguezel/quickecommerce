import CustomLayout from "@/components/layout/CustomLayout";
import { Card } from "@/components/ui";

const HomePage = () => {
  return (
    <CustomLayout>
      <Card className="p-2 md:p-6 ">Admin Dashboard</Card>
    </CustomLayout>
  );
};

export default HomePage;